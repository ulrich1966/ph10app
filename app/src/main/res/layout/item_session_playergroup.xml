<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/session_listitem_playergroup"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/txt_playergroup_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin"
        android:hint="@string/groupname"
        android:textSize="@dimen/big"
        app:layout_constraintBottom_toBottomOf="@+id/cmd_start_session"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/cmd_start_session" />

    <TextView
        android:id="@+id/cmd_players_show"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/no_margin"
        android:layout_marginTop="@dimen/margin"
        android:text="@string/players_show_for_groups"
        android:textColor="@color/colorBlue"
        android:autoLink="all"
        app:layout_constraintStart_toStartOf="@+id/txt_playergroup_name"
        app:layout_constraintTop_toBottomOf="@+id/txt_playergroup_name" />

    <ImageButton
        android:id="@+id/cmd_start_session"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin"
        android:tooltipText="@string/start_session"
        android:background="@android:color/transparent"
        android:onClick="actionStartSession"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_cards_48" />

</android.support.constraint.ConstraintLayout>
